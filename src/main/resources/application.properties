server.port=80
#连接池配置
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/admin?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.password=admin
spring.datasource.username=root
spring.datasource.type=com.zaxxer.hikari.HikariDataSource
# # 等待连接池分配链接的最大时长 默认30秒
spring.datasource.hikari.connection-timeout=30000
# 最小连接数
spring.datasource.hikari.minimum-idle=5
# 最大连接数
spring.datasource.hikari.maximum-pool-size=15
# 自动提交
spring.datasource.hikari.auto-commit=true
# 一个连接最长时间（ms) 默认10min
spring.datasource.hikari.idle-timeout=600000
# 连接池名字
spring.datasource.hikari.pool-name=DatebookHikariCP
# 一个连接的生命时长（ms）
spring.datasource.hikari.max-lifetime=28740000
spring.datasource.hikari.connection-test-query=SELECT 1

#mybatis配置
mybatis.type-aliases-package=cn.wsq.entity
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.config-location=classpath:mybatis/mybatis-config.xml

server.tomcat.uri-encoding=UTF-8
#上传图片保存的位置
WebImgServerUrl=http://localhost:8080/ImgWeb/upload/
#用户注册默认的头像
Faceicon=images/head/1.jpg



